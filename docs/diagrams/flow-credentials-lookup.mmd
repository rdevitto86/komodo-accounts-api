sequenceDiagram
    participant A as auth-api
    participant P as customer-api (private)
    participant C as in-proc cache (60s)
    participant D as DynamoDB

    A->>P: GET /v1/users/credentials?email=alice@example.com
    P->>C: lookup(email)
    alt cache hit
        C-->>P: CredentialsResponse
    else cache miss
        P->>D: Query GSI1 (EMAIL#alice@example.com → PROFILE)
        D-->>P: { customer_id }
        P->>D: GetItem (CUSTOMER#cust_…, PROFILE)
        D-->>P: profile + password_hash
        P->>C: store(email, response, ttl=60s)
    end
    P-->>A: CredentialsResponse { user_id, email, auth_methods, password_hash }
